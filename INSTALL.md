# Install Codex CLI

Install (Homebrew):

```bash
brew install --cask codex
```

Then:

```bash
cd ~/Documents/dadr3x_command_center_pro
codex
```

Copy this kit into your repository:

```bash
cp -R ~/Downloads/StreamPilot_Codex_Workflow_Kit/. .
```

First prompt:

Read AGENTS.md, PROJECT.md, TASKS.md, ai/AI_CONTEXT.md and docs/SPS/. Run Task 1 only. Do not modify code.
