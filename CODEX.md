# Codex CLI Workflow

Run `codex` in the repository.

Opening prompt:

Read AGENTS.md, PROJECT.md, TASKS.md, ai/AI_CONTEXT.md and docs/SPS/. Never rewrite the project. Work in small reviewable changes. Preserve creator workflows.
